# PhotoFold processor

Phase 0 provides only a deterministic health endpoint, codec doctor, dataset validator, and OpenAPI exporter. Image alignment, encoding, reconstruction, metrics, and jobs are deliberately absent.

